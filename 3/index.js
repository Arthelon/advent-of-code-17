// WIP
function findSteps(n) {}

function generateSpiral(n) {
  let grid = [[1]];
  let currRow = 0;
  let currCol = 0;
  for (let i = 2; i <= n; i++) {
    if (grid.length - 1 === currRow && currCol === grid[currRow].length - 1) {
    }
  }
}
